Implement new JDBC 4 metadata API getFunctionParameters()
FI am currently implementing this to return an empty result set so at least we re compliant, but we should be able to provide real metadata here.
