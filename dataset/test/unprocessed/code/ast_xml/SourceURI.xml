<Class>
    <Id>260</Id>
    <Package>groovy.transform</Package>
    <ClassName>SourceURI</ClassName>
    <ClassComment>SourceURI  /** 
 * Variable annotation used for getting the URI of the current script. &lt;p&gt; The type of the variable annotated with  {@code @BaseScript} must be assignment compatible with {@link java.net.URI}. It will be used to hold a URI object that references the source for the current script. &lt;/p&gt;&lt;p&gt;By default the URI will be made absolute (which is to say it will have an authority) in the case where a relative path was used for the source of the script.  If you want to leave relative URIs as relative, then set &lt;code&gt;allowRelative&lt;/code&gt; to &lt;code&gt;true&lt;/code&gt;. &lt;/p&gt; Example usage: &lt;pre&gt; {@code @groovy.transform.SourceURI} def sourceURIassert sourceURI instanceof java.net.URI &lt;/pre&gt;
 * @author Jim White
 * @since 2.3.0
 */
</ClassComment>
    <FieldList/>
    <MethodList/>
</Class>