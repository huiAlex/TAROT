<Class>
    <Id>863</Id>
    <Package>groovy.lang</Package>
    <ClassName>GrabResolver</ClassName>
    <ClassComment>GrabResolver  /** 
 * Used to add a repository for resolving Grape dependencies. &lt;p&gt; For example: &lt;pre&gt; {@code @GrabResolver}(name='restlet.org', root='http://maven.restlet.org') {@code @Grab}(group='org.restlet', module='org.restlet', version='1.1.6') class MyRestlet extends org.restlet.Restlet { // ... } &lt;/pre&gt; By default, the Grapes subsystem uses an Ivy chained resolver. Each resolver added using  {@code @GrabResolver} is appended to the chain. By default, the grapesubsystem is shared globally, so added resolvers will become available for any subsequent grab calls. Dependency resolution follows Ivy's artifact resolution which tries to resolve artifacts in the order specified in the chain of resolvers. &lt;p&gt; Further information about customising grape behavior can be found on the Grape documentation page: &lt;a href="http://groovy.codehaus.org/Grape"&gt;http://groovy.codehaus.org/Grape&lt;/a&gt;.
 * @author Merlyn Albery-Speyer
 */
</ClassComment>
    <FieldList/>
    <MethodList/>
</Class>