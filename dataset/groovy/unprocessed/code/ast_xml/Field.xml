<Class>
    <Id>987</Id>
    <Package>groovy.transform</Package>
    <ClassName>Field</ClassName>
    <ClassComment>Field  /** 
 * Variable annotation used for changing the scope of a variable within a script from being within the run method of the script to being at the class level for the script. &lt;p&gt; The annotated variable will become a private field of the script class. The type of the field will be the same as the type of the variable. Example usage: &lt;pre&gt; {@code @Field} List awe = [1, 2, 3]def awesum() { awe.sum() } assert awesum() == 6 &lt;/pre&gt; In this example, without the annotation, variable &lt;code&gt;awe&lt;/code&gt; would be a local script variable (technically speaking it will be a local variable within the &lt;code&gt;run&lt;/code&gt; method of the script class). Such a local variable would not be visible inside the &lt;code&gt;awesum&lt;/code&gt; method. With the annotation, &lt;code&gt;awe&lt;/code&gt; becomes a private List field in the script class and is visible within the &lt;code&gt;awesum&lt;/code&gt; method.
 * @author Paul King
 * @since 1.8.0
 */
</ClassComment>
    <FieldList/>
    <MethodList/>
</Class>