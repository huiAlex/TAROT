/**
     * Creates the corresponting Rete network for the given Rule and adds it to
     * the given rule base.
     * 
     * @param rule
     *            The rule to add.
     * @param kBase
     *            The rulebase to add the rule to.
     *            
     * @return a List of terminal nodes for the rule             
     *      */

/**
     * Adds a query pattern to the given subrule
     */

