p : Person( p.name ==  bob  )
Some users may write rules this way and it can help with code generation, so that you can use the same statements in the field constraints as you can in the eval (when using something like MVEL).
