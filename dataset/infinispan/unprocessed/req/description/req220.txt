The current JGroupsChannelLookup interface -   - does not have a mechanism to receive any configuration props.  When configuring a transport, Infinispan allows arbitrary &lt;properties /&gt; to be set (see   ).  These should simply be passed in to JGroupsChannelLookup.getJGroupsChannel().
