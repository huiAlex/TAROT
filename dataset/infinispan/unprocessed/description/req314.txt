we need to extend the query language by adding full-text operators
we need a way to configure analysers for fields in the protobuf schema, similar to the ones from hibernate-search
keyword and fuzzy querying wanted
